seata:
  application-id: ${spring.application.name}
  tx-service-group: default_tx_group
#  use-jdk-proxy: true
#  enable-auto-data-source-proxy: false
  config:
    type: nacos
    nacos:
      server-addr: ${kd.nacos.server.url}
      group: ${kd.seata.nacos.group}
      data-id: ${kd.seata.nacos.data-id}
      namespace: ${kd.seata.nacos.namespace}
  registry:
    type: nacos
    nacos:
      application: seata-server
      server-addr: ${kd.nacos.server.url}
      group: ${kd.seata.nacos.group}
      cluster: default
      namespace: ${kd.seata.nacos.namespace}